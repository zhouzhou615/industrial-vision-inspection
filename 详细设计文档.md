# 工业视觉比对检测系统 · 详细设计文档

**版本** V1.0 　**日期** 2026-06-08 　**技术栈** Spring Boot 3.2 + OpenCV 4.9 + 工业相机

---

## 1. 概述

### 1.1 目的
本文档面向开发、测试与现场实施人员，描述「标准图 + 工业相机采图 + 自动比对 + OK/NG 输出」系统的需求、架构、详细设计、接口、算法、部署与运维，并配套完整可运行的 Java 工程源代码与 Web HMI。

### 1.2 业务场景
产线对工件外观进行快速判定：事先录入一张「正确」标准图，工位到位后由工业相机拍照，系统自动与标准图比对，输出 **OK（正确）** 或 **NG（不合格）**，结果可上报 PLC / MES，并存档追溯。

### 1.3 术语
| 术语 | 含义 |
|------|------|
| 标准图 / 模板 | 录入的合格基准图（每产品编码一张） |
| ROI | 感兴趣区域，仅对该矩形做比对 |
| 相似度 | 待测图与标准图的综合相似程度（0~1） |
| 阈值 | 判定 OK 的最小相似度（默认 0.92） |
| OK / NG | 合格 / 不合格 |

---

## 2. 需求分析

### 2.1 功能需求
| 编号 | 需求 | 说明 |
|------|------|------|
| FR-01 | 标准图管理 | 按产品编码保存/更新基准图，支持 ROI |
| FR-02 | 相机采图 | 软触发采图，可配置曝光、增益 |
| FR-03 | 图像比对 | 相似度计算，超过阈值判 OK |
| FR-04 | 结果输出 | REST JSON；GPIO/PLC 输出 OK/NG |
| FR-05 | 追溯存档 | 保存采图、差异图、检测时间与相似度 |
| FR-06 | 离线调试 | 支持上传图片代替相机采图；Web HMI |

### 2.2 非功能需求
- **节拍**：单次检测 ≤ 500ms（视分辨率与算法可调）。
- **稳定性**：7×24 运行，相机断线可告警；采图失败自动重试 1 次。
- **可配置**：阈值、ROI、相机类型可配置，无需改代码。
- **可扩展**：相机 SDK、比对算法、信号输出均可插拔。

### 2.3 约束与假设
- 工位固定工装，产品位置偏差 ±30 像素内（模板匹配可校正）。
- 光照尽量恒定，必要时加环形光源。
- 标准图与待测图分辨率一致，或系统自动缩放对齐。

---

## 3. 系统架构设计

### 3.1 总体架构
```
┌─────────────┐     ┌──────────────────────────────────────────┐
│  Web HMI /  │────▶│           视觉检测服务 (Spring Boot)        │
│  PLC / MES  │     │  Controller → Service → Compare/Camera     │
└─────────────┘     └───────┬──────────────────┬───────────────┘
                            │                  │
              ┌─────────────▼─────┐    ┌───────▼────────┐
              │  工业相机 (GigE)   │    │  标准图库/ROI    │
              │  海康/Basler SDK  │    │  ./data/templates│
              └───────────────────┘    └────────────────┘
                            │
              ┌─────────────▼─────┐
              │ PLC / GPIO / MES  │
              │   OK / NG 信号     │
              └───────────────────┘
```

### 3.2 技术选型
| 层次 | 技术 | 理由 |
|------|------|------|
| 应用框架 | Spring Boot 3.2 | 成熟，易集成 REST 与配置 |
| 图像处理 | OpenCV 4.9 (Java, openpnp 自带 native) | 模板匹配、滤波、相似度计算，免安装 |
| 相机 | 抽象接口 + Mock/USB/厂商 SDK | 调试与产线解耦 |
| 前端 | 原生 HTML + Canvas | 单文件 HMI，支持 ROI 框选 |
| 部署 | Windows 工控机 / Linux | JAR 单进程 |

### 3.3 模块划分
1. **camera**：工业相机抽象与实现（mock / opencv / 厂商）。
2. **template**：标准图与 ROI 管理。
3. **compare**：图像预处理与混合比对算法。
4. **service**：检测业务流程编排。
5. **signal**：OK/NG 物理信号输出。
6. **controller**：REST API + 图片服务 + 全局异常处理。

### 3.4 检测时序
```
PLC/HMI → POST /api/v1/inspect/{code}
  → 校验标准图存在
  → 相机 open/setExposure/setGain（首次）
  → softwareTrigger() → grabFrame()（失败重试 1 次）
  → 加载标准图 + ROI
  → 预处理（ROI 裁剪 → 灰度 → 高斯滤波）
  → 模板匹配对齐 → 混合相似度计算 → 生成差异图
  → 与阈值比较 → 输出 OK/NG 信号
  → 存档采图与差异图 → 返回 InspectResult JSON
```

---

## 4. 详细设计

### 4.1 核心类图
```
IndustrialCamera «interface»
   ├── MockIndustrialCamera        (provider=mock，默认)
   ├── OpenCvUsbCamera             (provider=opencv)
   └── HikvisionCamera/BaslerCamera (扩展)
CameraFactory                       选择当前启用相机

ImageComparator «interface»
   └── HybridImageComparator        混合算法
ImagePreProcessor                   ROI/灰度/滤波 (package-private)
CompareScore                        相似度 + 差异图 + 明细

TemplateManager                     标准图与 ROI 持久化
VisionInspectService                流程编排
SignalOutput «interface»
   └── LoggingSignalOutput          默认日志输出
InspectController / ImageController / GlobalExceptionHandler
VisionProperties                    配置绑定
```

### 4.2 数据存储结构
```
data/
  templates/{productCode}/
    template.jpg     # 标准图
    roi.json         # 可选 ROI
  captures/{productCode}/
    capture_yyyyMMdd_HHmmss_SSS.jpg
    diff/diff_yyyyMMdd_HHmmss_SSS.jpg
```

ROI 配置格式 `roi.json`：
```json
{ "x": 100, "y": 80, "width": 800, "height": 600 }
```

### 4.3 关键接口设计
**IndustrialCamera**：`open/close/isOpened/grabFrame/setExposure/setGain/softwareTrigger`。
**ImageComparator**：`CompareScore compare(Mat template, Mat captured, Optional<RoiRegion> roi)`。
**SignalOutput**：`outputOk/outputNg/reset`。
各接口均以 Spring Bean 形式注入，可通过 `@ConditionalOnProperty` 切换实现而不改业务代码。

### 4.4 配置项
| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| vision.camera.provider | mock | 相机实现 |
| vision.camera.exposure-us | 10000 | 曝光（微秒） |
| vision.camera.gain | 1.0 | 增益 |
| vision.compare.algorithm | hybrid | 比对算法 |
| vision.compare.similarity-threshold | 0.92 | OK 阈值 |
| vision.compare.max-offset-pixels | 30 | 允许位移 |
| vision.compare.enable-roi | true | 启用 ROI |
| vision.signal.ok-gpio-pin / ng-gpio-pin | 1 / 2 | 信号引脚 |

### 4.5 异常处理
| 场景 | 处理 |
|------|------|
| 无标准图 | HTTP 400，提示先注册模板 |
| 采图失败 | 重试 1 次，仍失败 HTTP 500 |
| 多/无相机 Bean | 启动期/调用期抛 IllegalState，提示检查 provider |
| ROI 越界 | 自动裁剪到图像范围 |
| 图片路径穿越 | ImageController 校验 base 前缀，返回 403 |

### 4.6 内存管理
OpenCV `Mat` 为堆外内存，所有中间 `Mat` 在使用后显式 `release()`，避免 7×24 运行内存泄漏（见 `HybridImageComparator` 与 `VisionInspectService` 的 finally 释放）。

---

## 5. 比对算法设计

### 5.1 预处理
ROI 裁剪 →（BGR→灰度）→ 高斯滤波 3×3 降噪。

### 5.2 混合相似度（hybrid）
```
综合相似度 = 0.40 × 像素相似度
          + 0.35 × 结构相似度(SSIM)
          + 0.25 × 模板匹配置信度
```
- **像素相似度**：`1 - mean(|template - captured|)/255`，对整体差异敏感。
- **结构相似度 SSIM**：基于均值/方差/协方差的局部相似度，对光照变化鲁棒。
- **模板匹配置信度**：`TM_CCOEFF_NORMED` 最大归一化相关值，应对小幅位移。

### 5.3 位移对齐
当 `max-offset-pixels > 0`，先用模板匹配在待测图中定位标准图最佳位置，截取等大区域再比对，补偿工装定位误差。

### 5.4 差异可视化
`absdiff` 后二值化（阈值 25）生成差异图，NG 时定位缺陷区域。

### 5.5 阈值标定
首次用标准 OK 件连拍 10 张，取相似度最低值减 0.05 作为阈值；连续检测 20 次确认无误判。

---

## 6. 接口说明

Base URL：`http://localhost:8088`

| 方法 | 路径 | 入参 | 返回 |
|------|------|------|------|
| POST | /api/v1/inspect/template/{code} | multipart image | {success,message} |
| POST | /api/v1/inspect/template/{code}/roi | JSON {x,y,width,height} | {success,message} |
| GET | /api/v1/inspect/template/{code}/exists | - | {success,data:bool} |
| GET | /api/v1/inspect/templates | - | ["SKU-001",...] |
| POST | /api/v1/inspect/{code} | - | InspectResult |
| POST | /api/v1/inspect/{code}/file | multipart image | InspectResult |
| GET | /api/v1/image/template/{code} | - | image/jpeg |
| GET | /api/v1/image/capture?path= | 相对采图目录路径 | image/jpeg |

InspectResult 字段：productCode、passed、similarity、threshold、algorithm、message、templatePath、capturePath、diffImagePath、inspectTime、elapsedMs。

---

## 7. Web HMI 设计

单页 `static/index.html`，三步式操作：
1. **产品 / 标准图**：输入编码、上传标准图、下拉切换已注册产品。
2. **ROI 框选**：Canvas 上拖拽框选（自动换算原始像素坐标），保存到服务端。
3. **触发检测**：相机采图或上传图片；结果区显示 OK/NG 大字横幅、相似度/阈值/算法/耗时指标，以及标准图 / 采图 / 差异图三联预览。

---

## 8. 部署手册

### 8.1 环境
JDK 17+、Maven 3.8+、Windows/Linux、工业相机及 SDK（产线）。

### 8.2 构建运行
```bash
mvn clean package -DskipTests
java -jar target/industrial-vision-inspection-1.0.0.jar
# 访问 http://localhost:8088/
```

### 8.3 硬件安装建议
1. 相机固定，镜头垂直工件关键面。
2. 环形光源，避免阴影与反光。
3. 工装定位重复性 ≤ 0.5mm。
4. 首次拍 10 张标准件标定阈值。

### 8.4 产线集成
- PLC 触发：DI 上升沿 → 工控机 HTTP 触发检测。
- MES：解析 InspectResult JSON 上报。
- 看板：展示最近一次 similarity 与 passed。

### 8.5 常见问题
| 现象 | 处理 |
|------|------|
| 误判 NG 多 | 降低阈值 / 缩小 ROI / 改善光照 |
| 位移大 | 增大 max-offset-pixels 或改进工装 |
| 相机打不开 | 检查 provider 与 SDK 安装 |

---

## 9. 测试设计

| 用例 | 步骤 | 预期 |
|------|------|------|
| 相同图 | 标准图作待测图 | similarity>0.9，OK |
| 差异图 | 明显不同图 | similarity 明显降低，NG |
| 无模板 | 未注册即检测 | HTTP 400 |
| ROI 生效 | 设小 ROI 仅含关键区 | 仅该区域影响判定 |
| 离线上传 | 上传 NG 件 | 返回 NG 且生成差异图 |

单元测试见 `HybridImageComparatorTest`。

---

## 10. 安全与扩展

- 模板上传可加管理员 Token（扩展）。
- 检测接口可对 PLC 网段白名单。
- 迟滞策略：连续 N 次 NG 才告警（扩展）。
- 算法可替换为深度学习（缺陷分类/分割）实现 `ImageComparator` 即可。

---

*附：完整 Java 源代码见工程 `src/` 目录；接口与配置见 README.md。*
