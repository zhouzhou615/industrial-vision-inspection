# 换电脑部署清单（工业视觉比对检测系统）

> 目标：把整套系统迁到新电脑并跑起来。分「必装环境」「拷贝内容」「相机/硬件」「启动」「验证」五步。

---

## 一、必装环境（新电脑）

| 软件 | 版本 | 用途 | 备注 |
|------|------|------|------|
| **JDK** | **17 或更高** | 运行 Java 主程序 | 必须 ≥17，JDK 11 编译不过 |
| **Maven** | 3.8+ | 构建打包 | 若直接拷 jar 运行可不装 |
| **Python** | 3.8+（推荐 Anaconda） | 相机采图服务 | 必须 64 位 |
| **海康 MVS** | 4.6.3（与原机一致） | 相机驱动 + SDK | 官网 hikrobotics.com 下载 |
| **IntelliJ IDEA** | 任意 | 改代码/运行（可选） | 只跑不改可不装 |

### Python 依赖
```bash
pip install numpy opencv-python pyserial
```

### 环境变量确认
- `java -version` → 显示 17+
- `python -c "import platform;print(platform.architecture())"` → 显示 64bit

---

## 二、拷贝内容（从旧电脑）

拷贝整个 `industrial-vision-inspection` 文件夹，但注意：

| 目录/文件 | 是否要拷 | 说明 |
|-----------|---------|------|
| `src/` | ✅ **必须** | 全部源代码 |
| `pom.xml` | ✅ **必须** | Maven 配置 |
| `python-camera/` | ✅ **必须** | 相机采图服务脚本 |
| **`data/`** | ✅ **强烈建议** | **标准图 + 示教配置(spec.json)**，不拷要重新注册和示教全部螺丝/Logo |
| `*.md` 文档 | ✅ 建议 | 设计文档、指南 |
| `target/` | ❌ 不用 | 编译产物，新机重新编译 |
| `.idea/` | ❌ 不用 | IDE 配置，自动生成 |

> ⚠️ **`data/` 最重要**：里面是你辛苦调好的标准图、螺丝位、Logo 区、阈值。丢了要全部重做。

---

## 三、相机与硬件

1. **装 MVS 4.6.3**，先用 MVS 客户端确认相机能出图（相机独占，测完关掉 MVS）。
2. **网卡设置**（GigE 相机必做）：
   - 网卡设与相机同网段静态 IP
   - **巨帧 Jumbo Frame 开到 9014**
   - 接收缓冲区调最大
3. **确认 SDK 路径**，若与原机不同，改 `python-camera/hik_camera_server.py` 顶部：
   ```python
   SDK_MVIMPORT = r"D:\Program Files\MVS\MVS\Development\Samples\Python\MvImport"
   ```
   运行库一般在 `C:\Program Files (x86)\Common Files\MVS\Runtime\Win64_x64`（脚本会自动找）。
4. **传感器**接相机 Line0；**三色灯**接继电器模块（记下新机的 **COM 口号**）。

---

## 四、启动

### 1. 启动 Java 主程序
```bash
cd industrial-vision-inspection
mvn clean package -DskipTests
java -jar target/industrial-vision-inspection-1.0.0.jar
```
或在 IDEA 里运行 `VisionInspectionApplication`。

- 端口看 `src/main/resources/application.yml` 的 `server.port`（当前 **8080**）
- 网页：**http://localhost:8080/**

### 2. 启动 Python 采图服务
```bash
cd industrial-vision-inspection\python-camera
python hik_camera_server.py --product SKU-001 --java-url http://127.0.0.1:8080 --strobe 0 --light 1 --relay-port COM7 --relay-baud 9600 --exposure 40000 --gain 15
```

> ⚠️ **三处必须和新电脑一致**：
> - `--java-url` 端口 = Java 的 `server.port`
> - `--relay-port` = 新机继电器的 COM 口（设备管理器查）
> - `--exposure/--gain` = 新环境光照下重新调

---

## 五、验证

1. 浏览器开 `http://localhost:8080/` → 能看到界面
2. `http://127.0.0.1:5000/grab` → 能看到相机图像
3. 产品下拉里有 `SKU-001`，标准图和螺丝位显示正常（说明 `data/` 拷贝成功）
4. 过一个工件 → 看板出结果、灯变色

---

## 六、常见问题

| 现象 | 原因 | 处理 |
|------|------|------|
| Java 编译报错 | JDK 版本 <17 | 装 JDK 17+ |
| `ModuleNotFoundError: MvCameraControl_class` | SDK 路径不对 | 改脚本顶部 `SDK_MVIMPORT` |
| `Could not find module 'MvCameraControl.dll'` | 运行库路径 | 确认 `Common Files\MVS\Runtime\Win64_x64` 存在 |
| `打开设备失败 ret=0x80000203` | 相机被占用 | 关 MVS、结束残留 python、断电重连相机 |
| 网页没有产品/标准图 | 没拷 `data/` | 从旧机拷贝 `data/` 目录 |
| Python 连不上 Java | 端口不一致 | `--java-url` 端口对齐 `application.yml` |
| 灯不亮 | COM 口/波特率不对 | 设备管理器查 COM 口，用串口助手 HEX 发 `01 06 00 01 00 01 19 CA` 测试 |
| 检测不准 | 光照/位置与旧机不同 | **重新拍标准图、重新示教、重调阈值** |

---

## 七、重要提醒

**换电脑后光照环境和相机安装位置很可能变了**，即使拷了 `data/`，也建议：
1. 用新环境**重新拍一张标准图**并注册
2. **重新示教**螺丝位/Logo
3. **重新调阈值**（有螺丝 vs 没螺丝的分数重新测一遍）

检测参数是和「相机位置 + 光照」绑定的，环境变了参数就要重调。
