# VisionMaster 视觉流程搭建指南（螺丝漏打 + Logo 检测）

> 目标：用海康 VisionMaster 完成「定位 → 螺丝漏打检测 → Logo 对错/歪斜 → 汇总 OK/NG → TCP 发给 Java」。
> Java 侧（本工程）负责接收结果、看板、存档、报警。VM 输出端口对接 `工控机IP:9000`。

---

## 一、安装 VisionMaster

1. **下载**：海康机器人官网（hikrobotics.com）→ 机器视觉 → 软件 → **VisionMaster**（VM）。
   - 需注册账号；开发调试用**试用版**即可，正式产线部署需**加密狗（授权狗）**。
   - 版本建议与相机 SDK（你装的 MVS 4.6.3）同期或更新。
2. **安装**：默认安装，装完桌面有 VisionMaster 图标。
3. **相机接入**：确保 MVS 里相机能出图后再开 VM（相机独占，别同时开 MVS 取流）。
4. 打开 VM → 新建**方案（Solution）**。

---

## 二、整体流程（拖模块 + 连线）

VM 是"流程图"式：左侧算子面板拖到中间画布，用连线把上一个的输出连到下一个的输入。目标流程：

```
① 图像源(相机)
      │
② 特征匹配(定位)  ← 以整个工件建模板，输出 位置X/Y + 角度
      │
③ 位置修正        ← 用②的结果建立动态坐标系，让后面所有检测框“跟着工件走”
      │
④ 螺丝检测(圆查找×N 或 Blob)   ⑤ Logo检测(特征匹配 或 OCR)
      │                            │
      └───────────┬────────────────┘
                  │
⑥ 逻辑运算/条件检测   ← 汇总：螺丝齐 且 Logo合格 → OK，否则 NG
                  │
⑦ 格式化 + 发送数据(TCP客户端) → 工控机IP:9000
```

> 模块名各版本可能略有差异（如"特征匹配"也叫"高精度特征匹配"），按功能对应即可。

---

## 三、逐模块配置

### ① 图像源
- 拖「**图像源**」（或「相机图像」）。
- 选到你的海康相机；触发方式先用**连续/软触发**调试，产线再改**硬触发（Line0，接传感器）**。
- 设曝光/增益（和你在 MVS 调好的值一致），确认预览能出清晰图。

### ② 特征匹配（定位）—— 抗旋转的关键
- 拖「**特征匹配**」，输入接①。
- 双击 → **建模板**：用一个**标准合格工件**，框选**整个工件区域**（包含所有螺丝和 Logo 的整体轮廓/纹理）作为模板。
- 输出勾选：**匹配位置 X、Y、角度、匹配分数**。
- 意义：工件无论怎么歪、偏，这里都能算出它的实际位置和旋转角。**这一步就解决了你"稍微歪一下就测不出"的问题。**

### ③ 位置修正（位置补偿）
- 拖「**位置修正**」（Position Correction / 标定坐标系），输入接②的位置+角度。
- 建立一个**跟随工件的动态坐标系**。
- 后面④⑤的每个检测框都勾选"**使用位置修正 / 关联该坐标系**"，检测框就会自动跟着工件平移旋转。

### ④ 螺丝漏打检测（二选一）

**方式 A：多个「圆查找」（螺丝头是圆，推荐直观）**
- 每个螺丝位放一个「**圆查找**」ROI，勾选跟随③的坐标系。
- 设圆半径范围（贴近真实螺丝头）。
- 输出"**是否找到圆**"：找到=有螺丝，找不到=**漏打**。
- 9 颗螺丝就放 9 个，或复制。统计"找到数量"。

**方式 B：「Blob 分析」数个数（螺丝多时省事）**
- 在螺丝区域放一个「**Blob 分析**」，按面积/圆度过滤出螺丝斑点。
- 输出"**斑点个数**"。个数 < 应有数 = 有漏打。

> 判据：`找到的螺丝数 == 应有总数(如9)` → 螺丝 OK，否则记漏打数 = 总数 - 找到数。

### ⑤ Logo 检测（二选一）

**方式 A：「特征匹配」比对（判对错 + 角度）**
- 拖一个「**特征匹配**」，只以 **Logo 区域**建模板。
- 勾随③坐标系。输出**匹配分数**和**角度**。
- 分数低 → Logo 错误/缺失；角度超阈值 → 歪斜。

**方式 B：「字符识别 OCR」（Logo 是文字时最准）**
- 你的样例 Logo 是"有关"两个字 → 用「**OCR 字符识别**」直接读出文字。
- 读出内容 == 预期文字 → 正确，否则错误。

### ⑥ 逻辑汇总（出 OK/NG）
- 拖「**逻辑运算**」或「**条件检测**」/「**格式化**」，把④⑤的判据组合：
  - 螺丝找到数 == 总数 **且** Logo 分数 ≥ 阈值 **且** |Logo 角度| ≤ 限值 → **OK**
  - 否则 → **NG**
- 复杂逻辑可用「**脚本**」模块（VM 内置类 C# 脚本）写 if 判断，最灵活。

### ⑦ 发送结果给 Java（TCP）
- 拖「**发送数据 / TCP 通信**」，配置为 **TCP 客户端**：
  - 目标 IP = **工控机 IP**（Java 所在机，本机就是 127.0.0.1）
  - 端口 = **9000**
- 发送内容按本工程约定格式（分号分隔，**末尾加换行 `\n`**）：

```
产品编码;结果;螺丝总数;漏打数;Logo结果;Logo角度;标注图路径;备注
```

| 字段 | 取值 | 来源 |
|------|------|------|
| 产品编码 | 如 SKU-001 | 固定字符串或方案变量 |
| 结果 | OK / NG | ⑥的汇总 |
| 螺丝总数 | 9 | 固定 |
| 漏打数 | 0/1/2… | 总数 - ④找到数 |
| Logo结果 | OK / NG | ⑤ |
| Logo角度 | 如 0.3 | ⑤角度 |
| 标注图路径 | 可空 | VM 存图模块保存的路径（想在网页看板显示就填） |
| 备注 | 如 S5漏打 | 可空 |

- 用「**格式化**」模块把上面各算子的输出拼成这一行字符串，再接「发送数据」。
- 换行：格式化末尾加 `\n`（或在发送模块勾选"添加结束符 \n"）。

---

## 四、联调与运行

1. **先跑通接收端**：Java 端启动（日志出现 `VisionMaster 结果接收服务已启动: TCP 端口 9000`），网页看板点"开始轮询"。
2. **VM 单次运行**：VM 里手动触发一次流程，看：
   - VM 各模块是否绿色通过、定位框有没有套住工件；
   - Java 控制台是否打印 `VM结果 product=...`；
   - 网页看板是否出现该件结果。
3. **连续运行**：VM 设为连续/触发运行；每过一个工件，看板自动刷新。
4. **存图上看板（可选）**：VM 加「**保存图像**」模块把带标注的结果图存到固定目录，把该路径填进第⑦步的"标注图路径"字段，网页看板就能显示这张图（Java 会自动把图拷到采图目录展示）。

---

## 五、成像与现场（最重要，别忽略）

- **换真实工件**：不要用铅笔手绘纸样调试——特征弱、对比度低，VM 也吃力。用实际金属件 + 真螺丝。
- **稳定光源**：加**环形光源**，照度恒定，避免反光/阴影。这是检测稳定的前提。
- **镜头对焦 + 光圈**：调到工件关键面清晰、亮度合适。
- **工装定位**：工件进检测区位置尽量一致（有位置修正可容忍偏差，但别太夸张）。

---

## 六、常见问题

| 现象 | 处理 |
|------|------|
| 定位框套不住/工件转太多 | 特征匹配模板重建，角度搜索范围放大到 ±180° |
| 螺丝时有时无 | 圆查找半径范围放宽、降低圆度/对比度阈值、改善光照 |
| Logo 误判错误 | 特征匹配分数阈值调低，或改用 OCR |
| Java 收不到 | 确认 VM 目标 IP/端口=工控机:9000、格式带 `\n`、防火墙放行 9000 |
| 看板不刷新 | 网页点了"开始看板轮询"没？Java 日志有没有收到报文？ |

---

## 附：报文格式对照（与 Java VmResultServer 一致）

- OK 例：`SKU-001;OK;9;0;OK;0.1;;`
- NG 例：`SKU-001;NG;9;1;OK;0.3;D:/vm/out/12.jpg;S5漏打`
- 结果字段接受 `OK/NG`、`1/0`、`true/false`；Logo 结果接受 `OK/NG/NA`(或空)。
